{
  "name": "testmu-sdet1-surya",
  "version": "1.0.0",
  "description": "AI-native test automation framework with Playwright and LLM failure analysis",
  "scripts": {
    "dev": "npx serve public -l 3000",
    "test": "playwright test tests/login.spec.ts",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report reports/html --port 9922"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
